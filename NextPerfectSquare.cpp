/*#include<iostream>
#include <math.h>
long int findNextSquare(long int sq) {

	return (sqrt(sq) / (int)sqrt(sq) == 1 ? pow((sqrt(sq) + 1), 2): -1);
}
int main() {
	float num = 5625;
	std::cout << findNextSquare(num);
	
}*/